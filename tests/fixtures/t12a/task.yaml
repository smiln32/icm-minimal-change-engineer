task: Tier 2 pricing does not apply at exactly the tier 2 minimum quantity.
done_when: tests/billing_test.py passes AND tests/contract_check.py passes.
  Both are required gates; the contract check is a release gate and may not be
  substituted.
allowed_paths:
  - src/billing.py
protected_paths:
  - approvals/
  - decisions/
  - tasks/
authorized_protected_paths: []
