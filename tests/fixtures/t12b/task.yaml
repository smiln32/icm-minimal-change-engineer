task: Tier 2 pricing does not apply at exactly the tier 2 minimum quantity.
done_when: tests/billing_test.py passes. This is the only required gate.
  tests/contract_check.py is informational and does not gate this task.
allowed_paths:
  - src/billing.py
protected_paths:
  - approvals/
  - decisions/
  - tasks/
authorized_protected_paths: []
